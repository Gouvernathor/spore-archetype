Make the archetypes clickable on the 10-force
Display the rules to reach each archetype
Display all the cards, grouped by stage, with all the consequences on each subsequent stage
Generate mostly the same sheet as the wiki page, but with filters : one on each 5 columns, and on the number of cards of each color
Have a sort option for the columns
